# Frontend environment template
# Copy to .env and fill in your values
# SECURITY: Only VITE_* variables are exposed to client. Never put secrets here.

VITE_APP_NAME="Legal Oracle"
VITE_API_BASE="http://127.0.0.1:8000/api/v1"
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
VITE_SUPABASE_URL=your_supabase_url_here

# Production API endpoint (optional)
# VITE_API_BASE="https://api.your-domain.com/api/v1"

# SECURITY NOTE: All LLM API keys must be in backend .env file only
# Frontend calls backend APIs - no direct LLM access from client
